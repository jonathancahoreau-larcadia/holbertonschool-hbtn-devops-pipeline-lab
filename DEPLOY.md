# Staging Deployment Runbook

## Deployment target

The staging environment is hosted on Render.

The application is deployed as a Render Web Service using the Docker image
published to GitHub Container Registry (GHCR).

Docker images are published under:

```text
ghcr.io/jonathancahoreau-larcadia/holbertonschool-hbtn-devops-pipeline-lab
```
